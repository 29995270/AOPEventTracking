# AOPEventTracking
a simple example for umeng analytics. Use annotation to send custom event 
--
<br>
```java
    @EventTracking(eventId = "login")
    private void login() {
        //login
    }
```
